---
title: Cursor CLI
description: Configure Cursor CLI to use Superagent
---

Superagent provides seamless integration with Cursor CLI. Simply configure thee cli with the `--endpoint` flag:

```bash title="Cursor CLI Configuration"
cursor-agent --endpoint https://firewall.superagent.sh/{proxyId} # Replace with your Superagent proxy
```